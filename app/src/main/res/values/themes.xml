<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UnifyMobile" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/grey_regular</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorOnPrimary">@color/grey_regular</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDisablePreview">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="RecyclerViewStyle" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:scrollbarStyle">insideInset</item>
        <item name="android:scrollbarAlwaysDrawVerticalTrack">false</item>
        <item  name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_thumb_vertical_day</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_track_vertical_day</item>
    </style>

    <style name="DatePickerStyle" parent="Theme.AppCompat.Light.Dialog">
        <!--selected Item color-->
        <item name="android:colorControlActivated">@color/teal_700</item>
        <item name="android:textColorPrimary">@color/grey_regular</item>
        <item name="android:textColorPrimaryInverse">@color/grey_regular</item>
        <item name="android:textColorSecondaryInverse">@color/grey_light</item>
    </style>

    <style name="DialogAnimation" parent="Theme.AppCompat.DayNight">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>

    <style name="Theme.UnifyMobile.Splash" parent="Theme.Design.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>